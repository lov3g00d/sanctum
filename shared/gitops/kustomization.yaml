---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# No namespace transformer here: AppProject and ApplicationSet must stay in
# `argocd`, while the Applications they generate target `namespace: sanctum`. A
# namespace transformer would rewrite these CRs into the wrong namespace.
resources:
  - appproject.yaml
  - applicationset.yaml
