---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nimbus-orders-api
  namespace: nimbus
spec:
  template:
    spec:
      containers:
        - name: orders-api
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
