---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: podinfo
  namespace: nimbus
spec:
  template:
    spec:
      containers:
        - name: podinfo
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
