apiVersion: v1
kind: Service
metadata:
  name: icingaweb2-nodeport
  namespace: icinga
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: icingaweb2
    app.kubernetes.io/instance: icinga
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
